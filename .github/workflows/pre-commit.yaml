name: pre-commit

on:
  pull_request:
  push:
    branches: [Master]

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v1
